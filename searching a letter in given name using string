#include <stdio.h>
#include<string.h>
char string[50];
char k,i;

int main()
{
printf("Enter the name:");
fgets(string,sizeof(string),stdin);
printf("search here:");
scanf("%s",&k);

if(strchr(string,k)!=NULL)
{
    printf("present");
    
}
else
{
    
printf("not present");

}
}
